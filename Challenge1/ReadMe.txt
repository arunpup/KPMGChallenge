This installation process is performed using the following assumptions

1) Chosen Ansible for installation process as it is open source and all the prerequisite for ansible is taken care.
2) No firewall restrictions are in place for the port configurations of respective components. If firewall configuration exists then the same needs to be adjusted accordingly.
3) The playbook will be executed with the root privileges on RHEL server. 